services:
  db:
    image: postgres:15-alpine
    container_name: cesizen_postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=cesizen_db
    ports:
      - "5432:5432"
    volumes:
      - ./SQL/creation_table.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d cesizen_db"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ../../CESIZen_API
      dockerfile: Dockerfile
    container_name: cesizen_api
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=db;Port=5432;Database=cesizen_db;Username=postgres;Password=${POSTGRES_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
      - SMTP_FROM_NAME=CESIZen
    ports:
      - "5001:8080"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
